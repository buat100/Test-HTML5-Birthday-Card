<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Kartu ucapan ulang tahun yang indah dan interaktif untuk orang tersayang" />
  <meta name="theme-color" content="#0f172a" />
  <meta name="author" content="Dari suamimu" />

  <title>Selamat Ulang Tahun Sayang ❤️</title>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&family=Dancing+Script:wght@700&display=swap"
    rel="stylesheet"
  />

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: "Poppins", sans-serif;
      background: #0f172a;
      color: white;
      overflow-x: hidden;
    }

    section {
      min-height: 100vh;
      padding: 60px 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      position: relative;
    }

    h1, h2 {
      text-align: center;
    }

    #hero {
      background: linear-gradient(135deg, #0f172a, #1e293b);
      overflow: hidden;
    }

    .hero-content {
      z-index: 2;
      text-align: center;
    }

    .title {
      font-size: 3rem;
      font-family: "Dancing Script", cursive;
      margin-bottom: 20px;
      color: #ffd6e0;
      animation: fadeInDown 0.8s ease-out;
    }

    .typing {
      font-size: 1.2rem;
      max-width: 300px;
      margin: auto;
      min-height: 70px;
      line-height: 1.8;
      animation: fadeInUp 0.8s ease-out 0.2s both;
    }

    .open-btn {
      margin-top: 40px;
      border: none;
      padding: 15px 30px;
      border-radius: 50px;
      background: #ff4d6d;
      color: white;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.3s ease;
      animation: fadeInUp 0.8s ease-out 0.4s both;
      box-shadow: 0 4px 15px rgba(255, 77, 109, 0.3);
    }

    .open-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(255, 77, 109, 0.5);
    }

    .open-btn:active {
      transform: scale(0.98);
    }

    @keyframes fadeInDown {
      from {
        opacity: 0;
        transform: translateY(-30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes float {
      to {
        transform: translateY(-110vh) rotate(360deg);
        opacity: 0;
      }
    }

    .heart {
      position: fixed;
      bottom: -20px;
      color: #ff4d6d;
      animation: float linear forwards;
      opacity: 0.8;
      pointer-events: none;
      z-index: 1;
    }

    #memories {
      background: #111827;
    }

    .section-title {
      font-size: 2.5rem;
      margin-bottom: 50px;
      color: #ffd6e0;
      font-family: "Dancing Script", cursive;
      animation: fadeInDown 0.8s ease-out;
    }

    .memory-container {
      display: flex;
      flex-direction: column;
      gap: 30px;
      width: 100%;
      max-width: 400px;
    }

    .memory-card {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border-radius: 20px;
      overflow: hidden;
      transform: translateY(50px);
      opacity: 0;
      transition: 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    }

    .memory-card.show {
      transform: translateY(0);
      opacity: 1;
    }

    .memory-card img {
      width: 100%;
      height: 250px;
      object-fit: cover;
      display: block;
    }

    .memory-card p {
      padding: 20px;
      line-height: 1.8;
      text-align: center;
    }

    #reasons {
      background: #0f172a;
      text-align: center;
    }

    .reason-box {
      margin-top: 30px;
      padding: 30px;
      max-width: 350px;
      border-radius: 20px;
      background: rgba(255, 255, 255, 0.05);
      line-height: 1.8;
      font-size: 1.1rem;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      min-height: 100px;
      display: flex;
      align-items: center;
      justify-content: center;
      animation: fadeInUp 0.8s ease-out;
      transition: 0.5s ease;
    }

    .reason-box.fade-out {
      opacity: 0;
      transform: translateY(10px);
    }

    .reason-btn {
      margin-top: 20px;
      padding: 12px 24px;
      border: none;
      border-radius: 40px;
      background: #ff4d6d;
      color: white;
      cursor: pointer;
      font-size: 1rem;
      transition: 0.3s ease;
      box-shadow: 0 4px 15px rgba(255, 77, 109, 0.3);
    }

    .reason-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(255, 77, 109, 0.5);
    }

    .reason-btn:active {
      transform: translateY(0);
    }

    #letter {
      background: linear-gradient(180deg, #1e293b, #0f172a);
    }

    .letter-card {
      max-width: 700px;
      background: rgba(255, 255, 255, 0.05);
      padding: 40px;
      border-radius: 30px;
      line-height: 2;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
      animation: fadeInUp 0.8s ease-out;
    }

    .letter-card h2 {
      margin-bottom: 30px;
      font-size: 3rem;
      font-family: "Dancing Script", cursive;
      color: #ffd6e0;
    }

    .letter-card p {
      text-align: justify;
      font-size: 1.05rem;
    }

    #ending {
      background: #111827;
      text-align: center;
    }

    .ending-text {
      font-size: 3rem;
      font-family: "Dancing Script", cursive;
      color: #ff4d6d;
      margin-bottom: 20px;
      animation: fadeInDown 0.8s ease-out;
    }

    .small {
      opacity: 0.8;
      animation: fadeInUp 0.8s ease-out 0.2s both;
    }

    @media (max-width: 768px) {
      section {
        padding: 40px 15px;
      }

      .title {
        font-size: 2.5rem;
      }

      .section-title {
        font-size: 2rem;
      }

      .letter-card {
        padding: 25px;
      }

      .letter-card h2 {
        font-size: 2.5rem;
      }

      .ending-text {
        font-size: 2.5rem;
      }

      .typing {
        font-size: 1rem;
      }

      .open-btn, .reason-btn {
        font-size: 0.9rem;
        padding: 12px 20px;
      }
    }

    @media (max-width: 480px) {
      .title {
        font-size: 2rem;
      }

      .section-title {
        font-size: 1.5rem;
      }

      .ending-text {
        font-size: 2rem;
      }

      .letter-card {
        padding: 20px;
        line-height: 1.6;
      }

      .letter-card p {
        font-size: 0.95rem;
      }
    }
  </style>
</head>
<body>
  <audio id="bgm" loop preload="auto">
    <source src="music.mp3" type="audio/mpeg" />
    Browser Anda tidak mendukung audio HTML5.
  </audio>

  <section id="hero">
    <div class="hero-content">
      <h1 class="title">Selamat Ulang Tahun &#10084;&#65039;</h1>
      <p class="typing" id="typing" role="status" aria-live="polite" aria-atomic="true"></p>
      <button class="open-btn" id="openBtn" aria-label="Buka hadiah dan putar musik">
        Buka Hadiah 🎁
      </button>
    </div>
  </section>

  <section id="memories">
    <h2 class="section-title">Perjalanan Kita ✨</h2>
    <div class="memory-container">
      <div class="memory-card reveal">
        <img
          src="images/memory1.jpg"
          alt="Kenangan pertama kita bertemu"
          loading="lazy"
          onerror="this.src='https://via.placeholder.com/400x250?text=Memory+1'"
        />
        <p>
          Hari pertama kita bertemu, aku tidak tahu kalau kamu akan jadi orang
          paling penting di hidupku.
        </p>
      </div>

      <div class="memory-card reveal">
        <img
          src="images/memory2.jpg"
          alt="Kita bersama di saat sulit dan bahagia"
          loading="lazy"
          onerror="this.src='https://via.placeholder.com/400x250?text=Memory+2'"
        />
        <p>Terima kasih sudah selalu ada, di saat senang maupun sulit.</p>
      </div>

      <div class="memory-card reveal">
        <img
          src="images/memory3.jpg"
          alt="Rumah kita adalah dimana kita bersama"
          loading="lazy"
          onerror="this.src='https://via.placeholder.com/400x250?text=Memory+3'"
        />
        <p>Bersamamu, rumah terasa ada di mana saja &#10084;&#65039;</p>
      </div>
    </div>
  </section>

  <section id="reasons">
    <h2 class="section-title">Kenapa Aku Mencintaimu</h2>
    <div class="reason-box" id="reasonBox" role="region" aria-live="polite" aria-label="Alasan mengapa mencintaimu">
      Karena senyummu selalu bikin aku tenang &#10084;&#65039;
    </div>
    <button class="reason-btn" id="reasonBtn" aria-label="Tampilkan alasan lain">
      Alasan Lain ✨
    </button>
  </section>

  <section id="letter">
    <div class="letter-card">
      <h2>Untuk Kamu &#10084;&#65039;</h2>
      <p>
        Selamat ulang tahun, sayangku.
        <br /><br />
        Terima kasih sudah hadir dalam hidupku, membawa bahagia di setiap hariku,
        dan menjadi tempat pulang terbaik yang pernah aku punya.
        <br /><br />
        Aku tahu hidup tidak selalu mudah, tapi bersama kamu, semuanya terasa
        lebih indah.
        <br /><br />
        Semoga di umur yang baru ini, semua hal baik datang menghampirimu. Dan
        semoga aku tetap menjadi bagian dari setiap langkah bahagiamu.
        <br /><br />
        Aku mencintaimu, hari ini, besok, dan seterusnya &#10084;&#65039;
      </p>
    </div>
  </section>

  <section id="ending">
    <h1 class="ending-text">I Love You Forever &#10084;&#65039;</h1>
    <p class="small">&#10084;&#65039; Dari suamimu &#10084;&#65039;</p>
  </section>

  <script>
    const text = "Hari ini adalah hari spesial untuk wanita paling spesial di hidupku ✨";
    const typing = document.getElementById("typing");
    let index = 0;

    function typeText() {
      if (index < text.length) {
        typing.textContent = text.substring(0, index + 1);
        index++;
        setTimeout(typeText, 50);
      }
    }

    typeText();

    const button = document.getElementById("openBtn");
    const music = document.getElementById("bgm");

    button.addEventListener("click", () => {
      const playPromise = music.play();

      if (playPromise !== undefined) {
        playPromise
          .catch((error) => {
            console.log("Musik tidak bisa diputar:", error);
            alert(
              "Musik tidak bisa diputar. Pastikan browser memungkinkan audio autoplay."
            );
          })
          .then(() => {
            button.innerHTML = "I Love You &#10084;&#65039;";
            button.disabled = true;
          });
      }
    });

    let heartCount = 0;
    const MAX_HEARTS = 25;

    function createHeart() {
      if (heartCount >= MAX_HEARTS) return;

      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.innerHTML = "&#10084;&#65039;";
      heart.setAttribute("aria-hidden", "true");

      const randomLeft = Math.random() * 100;
      const randomSize = Math.random() * 20 + 10;
      const randomDuration = Math.random() * 3 + 4;

      heart.style.left = randomLeft + "vw";
      heart.style.fontSize = randomSize + "px";
      heart.style.animationDuration = randomDuration + "s";

      document.body.appendChild(heart);
      heartCount++;

      setTimeout(() => {
        heart.remove();
        heartCount--;
      }, randomDuration * 1000);
    }

    window.addEventListener("load", () => {
      setInterval(createHeart, 300);
    });

    const reasons = [
      "Karena senyummu selalu bikin aku tenang &#10084;&#65039;",
      "Karena kamu selalu percaya padaku ✨",
      "Karena bersamamu semuanya terasa lebih indah 💕",
      "Karena kamu adalah rumah bagiku 🏡",
      "Karena kamu selalu ada di setiap keadaan &#10084;&#65039;",
      "Karena aku tidak pernah bosan mencintaimu 💖",
      "Karena kamu membuat hari-hariku bermakna 🌟",
      "Karena tawamu adalah musik terindah bagiku 🎵",
      "Karena kamu adalah orang yang aku butuhkan &#10084;&#65039;",
      "Karena cinta padamu tidak akan pernah habis ✨",
    ];

    const reasonBox = document.getElementById("reasonBox");
    const reasonBtn = document.getElementById("reasonBtn");
    let lastIndex = -1;

    reasonBtn.addEventListener("click", () => {
      let randomIndex;

      do {
        randomIndex = Math.floor(Math.random() * reasons.length);
      } while (randomIndex === lastIndex);

      lastIndex = randomIndex;

      reasonBox.classList.add("fade-out");

      setTimeout(() => {
        reasonBox.innerHTML = reasons[randomIndex];
        reasonBox.classList.remove("fade-out");
      }, 300);
    });

    const reveals = document.querySelectorAll(".reveal");

    if ("IntersectionObserver" in window) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("show");
              observer.unobserve(entry.target);
            }
          });
        },
        {
          threshold: 0.1,
          rootMargin: "0px 0px -100px 0px",
        }
      );

      reveals.forEach((el) => observer.observe(el));
    } else {
      function revealOnScroll() {
        reveals.forEach((item) => {
          const windowHeight = window.innerHeight;
          const revealTop = item.getBoundingClientRect().top;

          if (revealTop < windowHeight - 100) {
            item.classList.add("show");
          }
        });
      }

      window.addEventListener("scroll", revealOnScroll);
      revealOnScroll();
    }

    if (!CSS.supports("scroll-behavior", "smooth")) {
      document.documentElement.style.scrollBehavior = "auto";
    }
  </script>
</body>
</html>
